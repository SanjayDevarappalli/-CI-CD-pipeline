# Web framework
Flask==3.0.3
flask-sqlalchemy==3.1.1
Flask-Migrate==4.0.7

# Database drivers
psycopg2-binary==2.9.9    # PostgreSQL (production)
# SQLite is built into Python (used for tests / local dev)

# Production WSGI server
gunicorn==22.0.0

# Testing
pytest==8.2.2
pytest-cov==5.0.0

# Linting
flake8==7.1.0
